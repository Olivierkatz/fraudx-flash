# Layout Components

Shell, navigation, and once-per-page layout components live here.

Use this slot for app shells, navigation chrome, persistent headers, split-pane
frames, and other structural components. Route-specific content should stay in
`src/views`; shared layout chrome belongs here once it repeats or becomes a
contract.

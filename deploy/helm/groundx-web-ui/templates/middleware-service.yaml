apiVersion: v1
kind: Service
metadata:
  name: {{ include "groundx-web-ui.fullname" . }}-middleware
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "groundx-web-ui.labels" . | nindent 4 }}
    app.kubernetes.io/component: middleware
spec:
  type: ClusterIP
  selector:
    {{- include "groundx-web-ui.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: middleware
  ports:
    - name: http
      port: {{ .Values.middleware.service.port }}
      targetPort: http
